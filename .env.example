# --- LLM (required) ---
# OpenAI API key. The agent reads this from the environment.
OPENAI_API_KEY=sk-...

# Optional: override the chat model (default: gpt-4o-mini)
CHAT_MODEL=gpt-4o-mini

# --- Server ---
# Local port. On Render, PORT is injected automatically.
PORT=3000
